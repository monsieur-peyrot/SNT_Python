dividende = float(input('Entrez le dividende : '))
diviseur = float(input('Entrez le diviseur : '))
resultat = dividende / diviseur
print('Le résultat de', dividende, 'divisé par', diviseur, 'est', resultat)